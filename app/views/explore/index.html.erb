<div class="ui centered grid">
  <%- @books.each do |b| %>
    <div class="four wide computer five wide tablet ten wide mobile column">
      <%= render 'book', book: book %>
    </div>
  <% end %>
</div>
